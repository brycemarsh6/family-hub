# Copy this file to `.env` and the app will run.
#   cp .env.example .env
#
# .env itself is never committed to git, because one day it will hold a real
# database password. This example file is the committed template.
DATABASE_URL="file:./prisma/dev.db"
